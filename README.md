# Trying `rollup-plugin-esbuild`

- [ ] make it work with `GLSL`
- [ ] add meaningful config to use esbuild only in `dev` mode
- [ ] see how fast does it build

This template is generated by `generator-rollup-tslib-starter`
